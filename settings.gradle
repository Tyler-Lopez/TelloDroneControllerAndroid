pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "TelloShare"
include ':app'
include ':core'
include ':corePresentation'
include ':auth'
include ':auth:authData'
include ':auth:authDomain'
include ':auth:authPresentation'
include ':navigation'
